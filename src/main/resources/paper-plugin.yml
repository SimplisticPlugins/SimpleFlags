name: '${name}'
main: '${group}.${name}'

version: '${version}'
api-version: '${apiVersion}'
description: '${description}'

loader: '${group}.SimpleStrap'

dependencies:
  server:
    WorldGuard:
      load: AFTER
      required: true
      join-classpath: true