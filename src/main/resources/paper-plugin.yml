name: '${name}'
main: '${group}.${name}'

version: '${version}'
api-version: '${apiVersion}'
description: '${description}'

dependencies:
  server:
    WorldGuard:
      load: AFTER
      required: true
      join-classpath: true